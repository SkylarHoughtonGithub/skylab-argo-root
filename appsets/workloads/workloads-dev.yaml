apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: workloads-dev
  namespace: argocd
spec:
  generators:
  - matrix:
      generators:
      - git:
          repoURL: https://github.com/SkylarHoughtonGithub/skylab-argo-workloads
          revision: HEAD
          directories:
          - path: workloads/*
          values:
            environment: dev
      - clusters:
          selector:
            matchLabels:
              environment: "dev"
          values:
            environment: dev
  template:
    metadata:
      name: '{{name}}-{{path.basename}}-{{values.environment}}'
      labels:
        cluster: '{{name}}'
        environment: '{{values.environment}}'
        component: workloads
        service: '{{path.basename}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/SkylarHoughtonGithub/skylab-argo-workloads
        targetRevision: HEAD
        path: '{{path}}/{{values.environment}}'
      destination:
        server: '{{server}}'
        namespace: '{{path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true