apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: backstage-infra-dev
  namespace: argocd
spec:
  generators:
  - matrix:
      generators:
      - git:
          repoURL: https://github.com/SkylarHoughtonGithub/skylab-backstage-configs
          revision: HEAD
          directories:
          - path: dev/*/*/*/infra/*
          values:
            environment: dev
      - clusters:
          selector:
            matchLabels:
              environment: "dev"
          values:
            environment: dev
  template:
    metadata:
      name: '{{name}}-{{path.basename}}-infra-{{values.environment}}'
      labels:
        cluster: '{{name}}'
        environment: '{{values.environment}}'
        component: infra
        service: '{{path.basename}}'
        layer: infra
    spec:
      project: default
      source:
        repoURL: https://github.com/SkylarHoughtonGithub/skylab-backstage-configs
        targetRevision: HEAD
        path: '{{path}}'
      destination:
        server: '{{server}}'
        namespace: '{{path.basename}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true